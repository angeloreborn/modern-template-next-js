export const invalid_body : Error = {
    name: "invalid body",
    message: "the supplied request was invalid"
}

export const invalid_token: Error = {
    name: "invalid token",
    message: "the supplied token was invalid"
}

export const invalid_credentials: Error = {
    name: "invalid credentials",
    message: "the supplied credentials were invalid"
}